# 数据库文件（自动生成，不提交）
/data/*.json
/data/*.db
# 但保留种子数据
!/data/workout_videos.json

# 密钥配置（含 API Key，不入库）
config.toml

# 本地审查工具生成物（可再生，不入库）
/review-delete.json
/review-workout-videos.csv
/review-workout-videos.html